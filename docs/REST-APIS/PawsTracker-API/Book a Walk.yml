info:
  name: Book a Walk
  type: http
  seq: 3

http:
  method: POST
  url: https://api.pawstracker.local/v1/bookings
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
