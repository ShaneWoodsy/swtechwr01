info:
  name: Register Walker
  type: http
  seq: 1

http:
  method: POST
  url: https://api.pawstracker.local/v1/walkers
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
